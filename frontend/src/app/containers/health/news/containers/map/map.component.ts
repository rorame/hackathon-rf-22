import { Component } from '@angular/core';

@Component({
  selector: 'map',
  styleUrls: ['map.component.scss'],
  template: `
    <div>
      Карта
    </div>
  `
})
export class mapComponent {
  constructor() {}
}
